<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <h1>Fruit Details</h1>
    <p><%= foundFruit.name %></p>

    <% if (foundFruit.isReadyToEat) { %>
        <p>Fruit is ready to eat!</p>
    <% } else { %>
        <p>Not ready to eat :(</p>
    <% } %>

    

    <a href="/fruits/<%= foundFruit._id %>/edit">Edit Fruit</a>
    <form action="/fruits/<%= foundFruit._id %>?_safa=DELETE" method="post">
        <button type="submit">Delete Fruit</button>
    </form>




    <!-- Exercise 1: use scriplet tags to write if statement that shows: 'Fruit is ready to eat!' if the fruit isReadyToEat is true and 'Not ready to eat :(' if false -->
</body>
</html>